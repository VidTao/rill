# Source: Dim Campaigns (with campaign_type)
# Ingests from ClickHouse dim table into DuckDB

type: source
connector: clickhouse
sql: SELECT * FROM ziva.dim_campaigns FINAL
refresh:
  cron: 0 * * * *
